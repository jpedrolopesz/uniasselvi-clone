/** Nome do cookie de usuário ativo — isolado em arquivo próprio para poder ser importado tanto por código de servidor quanto por Client Components (ver UserSwitcher). */
export const ACTIVE_USER_COOKIE = "active_user_id";
